name: Test Runner Resources
on: [push]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from free Ubuntu VM!"
      - run: df -h
      - run: free -h

